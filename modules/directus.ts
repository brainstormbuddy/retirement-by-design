import { Directus } from '@directus/sdk';

//@ts-ignore
// export default new Directus(process.env.NEXT_PUBLIC_API_URL);
// export default new Directus('http://localhost:8055');
export default new Directus('https://admin.retirementby.design');
